# 第一版本

## 数据集
将所有帧数控制在5、9帧，并且新增了一个growth_days字段，用于计算首帧到最后的天数
由于只有5、9帧，帧数很少，且尽量要保持平均，处理结果：
- 5帧：从1 3 5 7 9中获得。此时growth_days = 24
- 9帧：前1-8帧是从0-11帧中获得，9-11帧随机选择一帧，此时growth_days根据最后一帧来

## 模型

对于growth_days的处理，主要就是在这个地方

```
def apply_growth_days_condition_to_time_embedding(
    dit: WanModel,
    t: torch.Tensor,
    growth_days,
    num_frames: Optional[int] = None,
):
    # print("     [INFO] growth_days: ", growth_days)
    if growth_days is None:
        return t
    if torch.is_tensor(growth_days):
        growth_days_tensor = growth_days.to(device=t.device, dtype=t.dtype).flatten()
    else:
        growth_days_tensor = torch.tensor([float(growth_days)], device=t.device, dtype=t.dtype)
    frame_count = max(float(num_frames or 1) - 1.0, 1.0)
    days_per_frame = growth_days_tensor / frame_count
    growth_position = days_per_frame * 100.0
    # print("     [INFO] growth_position: ", growth_position)
    growth_emb = dit.time_embedding(sinusoidal_embedding_1d(dit.freq_dim, growth_position))
    # print("     [INFO] growth_emb: ", growth_emb, growth_emb.shape)
    if t.dim() == 3:
        return t + growth_emb[:1].unsqueeze(1)
    if growth_emb.shape[0] != t.shape[0]:
        growth_emb = growth_emb[:1].repeat(t.shape[0], 1)
    return t + growth_emb.to(dtype=t.dtype, device=t.device)
```

## 效果
全乱，调整lora的强度到0.6也是混乱的效果
甚至不加lora的时候都是全乱

## 错误分析

growth 注入太强，破坏了原模型的 timestep 语义
t_mod 会控制每个 DiTBlock 的 AdaLN 调制。
这相当于把“扩散时间步”整体偏移/扰乱了。LoRA 只训练了 q,k,v,o,ffn，没有专门训练 time_embedding/time_projection，所以很可能补不回来。

不要加到时间步中，改为单独处理（类似时间步编码）后加到context token后面

# 第二版本

## 数据集
与第一版一样，没有改动

## 模型

growth_days的处理方式仍然与timestep一致，还是放大了100倍。
不过没有直接加到t上面，而是与context token做拼接
主要使用交叉注意力机制
```
def append_growth_days_context_token(
    context: torch.Tensor,
    growth_days,
    num_frames: Optional[int] = None,
):
    if growth_days is None:
        return context
    if torch.is_tensor(growth_days):
        growth_days_tensor = growth_days.to(device=context.device, dtype=context.dtype).flatten()
    else:
        growth_days_tensor = torch.tensor([float(growth_days)], device=context.device, dtype=context.dtype)
    frame_count = max(float(num_frames or 1) - 1.0, 1.0)
    days_per_frame = growth_days_tensor / frame_count
    # 方案A：无新增可训练参数。把物理量 days/frame 固定编码成一个额外 context token，
    # 让 LoRA 通过 cross-attention 学会读取该条件，而不是改动扩散 timestep。
    growth_position = days_per_frame * 100.0
    growth_token = sinusoidal_embedding_1d(context.shape[-1], growth_position)
    growth_token = growth_token.to(device=context.device, dtype=context.dtype)
    if growth_token.shape[0] != context.shape[0]:
        growth_token = growth_token[:1].repeat(context.shape[0], 1)
    return torch.cat([context, growth_token.unsqueeze(1)], dim=1)
```

## 效果
整体没乱
天数控制力还是比较微弱
是否需要单独训练MLP？
干旱的效果还是没达到数据集中的样子
为什么9帧和49帧差别那么大？

## 错误分析

如果干旱视频里“卷叶、萎蔫、干尖”本身不明显，或者只在最后几帧才明显，那么训练 loss 会被背景、盆、植株整体形状、生长过程这些大面积内容淹没。LoRA 学到的就会是“玉米苗视频风格”，而不是强烈的干旱表型。

加了一个额外 context token，它是温和注入，不会破坏模型，但控制力天然不会特别强。一个 token 混在很多 prompt token 里面，LoRA 不一定会强烈关注它。

考虑使用MLP单独处理会怎么样，需不需要改一下prompt？？

# 第三版本

## 数据

修改prompt
正常 -> Greenhouse potted maize seedling, healthy green leaves, normal growth, humidity 50%, no water shortage.

干旱 -> Greenhouse potted maize seedling under drought stress, humidity 10%, water shortage, curled leaves, wilted leaves, dry leaf tips, slower growth.

## 模型

```text
不再用固定 sinusoidal growth token，
改成用一个可训练 MLP 单独处理 growth_days，
再把 MLP 输出作为额外 context token 拼到文本 context 后面。
```

也就是：

```text
growth_days + days_per_frame
        ↓
GrowthDaysEmbeddingMLP
        ↓
growth token
        ↓
拼到 context 后面
        ↓
原本 DiT cross-attention 读取它
```

**修改文件**

`diffsynth/models/wan_video_dit.py`

新增了：

```python
GrowthDaysEmbeddingMLP
```

输入是两个物理量：

```python
[growth_days / 100, days_per_frame / 10]
```

没有用 `max_growth_days`，避免换数据集还要改归一化上限。

同时新增：

```python
ensure_growth_embedding_mlp(...)
load_extra_lora_state_dict(...)
```

原因是不能把 MLP 直接写进 `WanModel.__init__`，否则加载官方 Wan2.2 权重时会出现 missing keys。现在是加载完官方模型后，再按需动态创建 MLP。

`diffsynth/pipelines/wan_video.py`


如果没有训练好的 MLP，会报错

`examples/wanvideo/model_training/train.py`

新增训练参数：

```bash
--train_growth_mlp
```

打开后会让：

```python
dit.growth_embedding_mlp
```

参与训练。也就是说这次仍然是 LoRA 训练流程，但训练参数变成：

```text
LoRA 参数 + growth_embedding_mlp 参数
```

`diffsynth/diffusion/base_pipeline.py`

修改了 `load_lora(...)`。

现在加载 LoRA checkpoint 时，会先检查里面有没有：

```text
growth_embedding_mlp.*
```

如果有，就先单独加载 MLP，再加载普通 LoRA。

`diffsynth/diffusion/training_module.py`

修改了 LoRA checkpoint 映射逻辑，保证加载已有 checkpoint 时不会把：

```text
growth_embedding_mlp.*
```

这些非 LoRA 参数丢掉。

`examples/wanvideo/model_training/lora/Wan2.2-TI2V-5B.sh`

新增：

```bash
--train_growth_mlp
```

并把输出目录改成：

```bash
./models/train/Wan2.2-TI2V-5B_lora_growth_mlp_context_token
```

旧目录保留为注释，避免和第二版 checkpoint 混用。

**为什么这样改**

第二版固定 sinusoidal token 比第一版安全，但表达能力弱。模型只能靠 LoRA 自己理解一个固定编码的数字 token。

第三版让 MLP 学习：

```text
growth_days / days_per_frame 应该如何变成模型容易读取的条件向量
```

但仍然不改 timestep，不新增单独 cross-attention，只复用原本 DiT 的 cross-attention，所以风险比改主干结构小。

**训练后权重**

训练输出的 `.safetensors` 里应该同时有：

```text
普通 LoRA 权重
growth_embedding_mlp.net.0.weight
growth_embedding_mlp.net.0.bias
growth_embedding_mlp.net.2.weight
growth_embedding_mlp.net.2.bias
```

推理时仍然用：

```python
pipe.load_lora(pipe.dit, lora_path, alpha=...)
```

它会自动加载 LoRA 和 MLP。

## 效果
干旱条件的更加明显了，可能是因为修改了提示词？
对天数控制还是没有太大的变化，整体和第二版也没有什么差别