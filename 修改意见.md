# 第四版 Growth Cross-Attention 修改计划

  ## Summary

  将 growth_days 从“追加到文本 context 的一个 token”升级为“单独编码后，通过独立 cross-attention 分支注入 DiT”。默认不改官方模型初始化行为，仍然动态挂载新模块，避免加载原始 Wan 权重时出现 missing keys。第三版的 [growth_days / 100,
  days_per_frame / 10] 特征归一化保留。

  ## Key Changes

  - 修改 diffsynth/models/wan_video_dit.py
      - 新增 GrowthDaysCrossAttentionInjector，结构参考现有 WanToDanceInjector。
      - 每个选中层包含：
          - LayerNorm(dim, elementwise_affine=False)
          - 独立 CrossAttention(dim, num_heads)
          - 可训练标量或向量 gate，初始为 0
      - 新增 ensure_growth_cross_attention(...)，动态创建第四版模块。
      - 新增 apply_growth_cross_attention(block_idx, hidden_states, growth_token)，在 DiT block 后执行：

        hidden_states = hidden_states + growth_gate * growth_cross_attn(norm(hidden_states), growth_token)

      - 扩展 load_extra_lora_state_dict，兼容加载：
          - growth_embedding_mlp.*
          - growth_cross_attention.*
          - pipe.dit.growth_cross_attention.*

  - 修改 diffsynth/pipelines/wan_video.py
      - build_growth_days_features 保留不变。
      - append_growth_days_mlp_context_token 不再把 growth token 拼到 context。
      - 新增函数生成 growth_token，返回给 model_fn_wan_video。
      - model_fn_wan_video 调用 DiT 时把 growth_token 传入，DiT block 循环中由模型侧独立 cross-attn 使用。
      - 如果传入 growth_days 但没有初始化第四版模块，报错提示需要训练/加载包含 growth_cross_attention.* 的 checkpoint。
  - 修改 examples/wanvideo/model_training/train.py
      - 新增训练参数：

        --train_growth_cross_attention

      - 开启后同时创建并训练：
          - growth_embedding_mlp
          - growth_cross_attention
      - 默认注入层使用 [0, 4, 8, 12, 16, 20, 24, 27]，和 WanToDance music injector 默认层保持一致。
      - 不开启该参数时，现有训练逻辑不受影响。

  - 修改 diffsynth/diffusion/training_module.py
      - 扩展 mapping_lora_state_dict，保证 LoRA checkpoint 中的 growth_cross_attention.* 不会被过滤掉。

  ## Behavioral Details

  - growth_days 编码仍使用：

    [growth_days / 100.0, days_per_frame / 10.0]

  - growth_embedding_mlp 输出形状为 [B, dim]，再变成 [B, 1, dim] 作为独立 cross-attn context。
  - 第四版不再把 growth token 拼接进文本 context，避免和 prompt token 共用同一套 cross-attention K/V 投影。
  - 新增 cross-attn 分支 gate 初始为 0，初始状态等价于原模型，降低扰动基模型的风险。
  - 官方 Wan / WanToDance 原始权重仍可正常加载；只有传入 growth_days 时才要求加载或训练第四版模块。

  ## Test Plan

  - 静态检查：
      - 导入 WanModel、WanVideoPipeline 不报错。
      - 不带 growth_days 的普通 Wan 推理路径不要求创建第四版模块。
  - 最小功能检查：
      - 调用 ensure_growth_cross_attention(trainable=True) 后确认参数 requires_grad=True。
      - 传入 dummy growth_days，确认 growth_token shape 为 [B, 1, dim]。
      - DiT block 前后 tensor shape 不变。
  - Checkpoint 检查：
      - LoRA/full checkpoint 能保存 growth_embedding_mlp.* 和 growth_cross_attention.*。
      - pipe.load_lora(...) 能恢复两类额外参数。
  - 训练验证：
      - 使用 WanToDance 小样本执行一次 dry-run 或极短训练，命令包含：

        --train_growth_mlp --train_growth_cross_attention

      - 用对应 validate 脚本加载 checkpoint，传入不同 growth_days，确认推理路径不报错。

  ## Assumptions

  - 第四版目标是替代第三版“追加 context token”的注入方式，而不是两者同时叠加。
  - 默认只支持 WanToDance/growth 实验路径，不改变普通 Wan pipeline 的公开行为。
  - 默认注入层采用 WanToDance 现有 music injector 的层列表 [0, 4, 8, 12, 16, 20, 24, 27]。
  - 新增模块继续作为 LoRA checkpoint 里的额外非 LoRA 参数保存和加载。